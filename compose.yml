services:
  nginx:
    image: nginx-image:latest
    ports:
      - "80:80"
      - "443:443"
    networks:
      - test
    volumes:
      - ./certs/letsencrypt:/etc/letsencrypt

  apache:
    image: apache-image:latest
    networks:
      - test

networks:
  test:
    driver: bridge
